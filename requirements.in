# Core local RAG pipeline, evaluation, reporting, and test dependencies.
# Ollama is the local runtime and is validated separately by preflight.
datasets
rank-bm25
jsonschema
pydantic
httpx
sseclient-py
numpy
matplotlib
pytest
