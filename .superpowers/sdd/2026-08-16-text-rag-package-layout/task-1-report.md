Status: completed

Scope completed:
- Moved text dataset materialization into `scripts/ingestion/materialize.py`.
- Moved text-corpus manifest helpers into `scripts/ingestion/corpus.py`.
- Added package exports in `scripts/ingestion/__init__.py`.
- Added module entrypoint `scripts/ingestion/run.py` for `python -m scripts.ingestion.run`.
- Moved materialization tests to `tests/ingestion/test_materialize.py`.
- Added `tests/ingestion/test_corpus.py` with the three ingestion-focused cases moved out of `tests/test_text_rag.py`, plus the CLI help check.
- Deleted the legacy top-level scripts `scripts/materialize_dataset.py` and `scripts/ingest_corpus.py`.
- Updated `TASKS.md` to mark T09a complete.

GitNexus impact analysis before edits:
- `materialize` (`scripts/materialize_dataset.py`): MEDIUM risk; 5 direct callers/tests, 1 affected process, 1 affected module.
- `ingest_from_materialization` (`scripts/ingest_corpus.py`): LOW risk; 2 direct callers/tests, 1 affected process, 1 affected module.
- `load_cached_passages` (`scripts/ingest_corpus.py`): LOW risk; 2 direct callers/tests, 1 affected process, 1 affected module.

TDD evidence:
- Red: `/Users/dmondal/Documents/week-1-fde/.venv/bin/python -m pytest tests/ingestion/test_materialize.py tests/ingestion/test_corpus.py -q`
  - Failed with `ModuleNotFoundError` for `scripts.ingestion.materialize` and `scripts.ingestion.corpus` before implementation existed.
- Green:
  - `/Users/dmondal/Documents/week-1-fde/.venv/bin/python -m pytest tests/ingestion/test_materialize.py tests/ingestion/test_corpus.py -q`
  - `/Users/dmondal/Documents/week-1-fde/.venv/bin/python -m scripts.ingestion.run --cache-root /private/tmp/t06-dataset-cache --output artifacts/text_evidence_manifest.v1.json`
- Extra confidence check:
  - `/Users/dmondal/Documents/week-1-fde/.venv/bin/python -m pytest tests/test_text_rag.py -q`

Validation results:
- Focused ingestion suite: 13 passed.
- Required CLI validation: succeeded; ingested 3,200 passages into `artifacts/text_evidence_manifest.v1.json`.
- Residual retrieval suite after test extraction: 3 passed.

Behavior preserved:
- Dataset revision, schema version, evidence ID format, and hashing behavior were copied unchanged.
- Offline Arrow-cache discovery and `dataset_from_file` injection behavior were preserved.
- `local_files_only` / `DownloadConfig` logic in `load_huggingface` was preserved.

Side effects:
- `artifacts/text_evidence_manifest.v1.json` was regenerated by the required validation command.

Anomalies / deviations:
- None.

Remaining concerns:
- The repo still contains historical documentation references to the deleted legacy script paths (for example in prior notes and the implementation plan), but no live code or active tests depend on them within Task 1 scope.

Commit(s):
- `262bd00` — `refactor(ingestion): group text corpus modules`

---

Corrective follow-up for review findings:

Issue addressed:
- High: removed the `scripts.ingestion.corpus` dependency on `scripts.text_rag` for `ingest_passages` by moving manifest-building ownership into `scripts.ingestion.corpus`.
- Medium: restored `test_ingestion_uses_only_the_pinned_materialized_text_corpus_identity` and the ingestion CLI help test to `tests/test_text_rag.py`, leaving only the three permitted ingestion cases in `tests/ingestion/test_corpus.py`:
  - manifest construction
  - malformed passages
  - cached Arrow discovery

Implementation details:
- Added `TextRagError`, `normalize_text`, manifest hashing helpers, and `ingest_passages()` directly to `scripts/ingestion/corpus.py`.
- Updated `scripts/text_rag.py` to consume the shared ingestion error/normalization contract while leaving retrieval/index behavior unchanged.
- Updated `scripts/ingestion/run.py` and package exports to use the ingestion-owned contract.
- Adjusted test ownership per the review findings.

GitNexus impact analysis before corrective edits:
- `ingest_passages` (`scripts/text_rag.py` before move): MEDIUM risk; 6 direct dependents/tests, 1 affected process, 1 affected module.
- `tests/test_text_rag.py`: LOW risk; no upstream dependents.

Corrective validation evidence:
- `/Users/dmondal/Documents/week-1-fde/.venv/bin/python -m pytest tests/ingestion/test_corpus.py tests/test_text_rag.py -q`
  - Result: 8 passed.
- `/Users/dmondal/Documents/week-1-fde/.venv/bin/python -m pytest tests/ingestion/test_materialize.py -q`
  - Result: 8 passed.
- `/Users/dmondal/Documents/week-1-fde/.venv/bin/python -m scripts.ingestion.run --cache-root /private/tmp/t06-dataset-cache --output artifacts/text_evidence_manifest.v1.json`
  - Result: succeeded; ingested 3,200 passages.

Pre-commit GitNexus detect_changes:
- LOW risk; no affected execution processes reported.

Follow-up anomalies / deviations:
- The initial “red” rerun for the corrective step stayed green because `tests/ingestion/test_corpus.py` already resolved `ingest_passages` through the delegated import in `scripts.ingestion.corpus`; that confirmed the review finding rather than blocking the fix.

Follow-up commit(s):
- `de80367` — `fix(ingestion): move manifest ownership`

---

Bookkeeping correction:

- Reverted `TASKS.md` item `T09a` from complete back to pending because `T09a` tracks the entire three-task package refactor and must remain unchecked until Task 3 completes.

Bookkeeping validation:

- Confirmed the change only affects the `T09a` checkbox state and this task report note.

Bookkeeping commit(s):

- `86567d9` — `docs(tasks): reopen package refactor tracker`
